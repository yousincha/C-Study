/*
#include <stdio.h>

#define MUL(x,y) ((x)*(y))

int mul(x, y);

int main(void)
{
	int a, b;
	printf("두 개의 정수를 입력 하세요: ");
	scanf_s("%d %d", &a, &b);

	printf("매크로 함수 호출 결과: %d \n", MUL(a + 1,b + 1));
	printf("일반 함수 호출 결과: %d \n", mul(a + 1, b + 1));
	return 0;
}

int mul(x, y)
{
	return x * y;
}

*/