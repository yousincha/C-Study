/*
#include <stdio.h>

#define PI 3.14

int main(void)
{
	double area, circum, radius;

	fputs("반지름을 입력하세요 : ", stdout);
	scanf_s("%lf", &radius);

	area = PI * radius * radius;
	circum = 2 * PI * radius;

	printf("원의 넓이: %lf \n", area);
	printf("원의 둘레: %lf \n", circum);

	return 0;
}
*/